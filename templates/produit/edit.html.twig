{% extends 'base.html.twig' %}

{% block title %}Edit Produit{% endblock %}

{% block body %}
    <h1>Edit Produit</h1>
<div class="container">
    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="container">
    <div class="row mt-4 ">
        <div class="col-1">
        <a href="{{ path('app_produit_index') }}" class="btn btn-primary">Back</a>
        </div><div class="col-1">
        {{ include('produit/_delete_form.html.twig') }}
    </div></div></div></div>

{% endblock %}
