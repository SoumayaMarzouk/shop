{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
    <h1>Fournisseur</h1>
<div class="container">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fournisseur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ fournisseur.nom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ fournisseur.tel }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row">
        <div class="col-1">
        <a href="{{ path('app_fournisseur_index') }}" class="btn btn-primary">Back</a>
        </div><div class="col-1">
        <a href="{{ path('app_fournisseur_edit', {'id': fournisseur.id}) }}" class="btn btn-success">Edit</a>
        </div><div class="col-1">
            {{ include('fournisseur/_delete_form.html.twig') }}
        </div></div></div>

{% endblock %}
