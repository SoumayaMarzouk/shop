{% extends 'base.html.twig' %}

{% block title %}Categorie{% endblock %}

{% block body %}
    <h1>Categorie</h1>
<div class="container">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ categorie.libelle }}</td>
            </tr>
        </tbody>
    </table>
    <div class="row">
        <div class="col-1">
        <a href="{{ path('app_categorie_index') }}" class="btn btn-primary">Back</a>
        </div><div class="col-1">
        <a href="{{ path('app_categorie_edit', {'id': categorie.id}) }}" class="btn btn-success">Edit</a>
        </div><div class="col-1">
            {{ include('categorie/_delete_form.html.twig') }}
        </div></div></div>


{% endblock %}
